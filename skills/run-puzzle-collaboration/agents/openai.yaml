interface:
  display_name: "实时解题协作"
  short_description: "优先即时分享可检验候选，再按需升级转录、工作簿记录和正式验证"
  default_prompt: "Use $run-puzzle-collaboration to solve this puzzle by sharing core observations immediately and escalating OCR, workbooks, and formal gates only when the task truly requires them."
